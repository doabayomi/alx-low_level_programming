#include <stdio.h>

/**
 * main - Main function of C
 *
 * Description: This program basically counts from 1 to 10
 * in base 10 (which is still no difference)
 * Return: 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
