#include <stdio.h>

/**
 * main - the main function of the program
 * @str[]: Defined string
 *
 * Description: Prints out the defined string in the standard
 * output
 * Return: zero value
 */
int main(void)
{
	/*The string is stored in the str variable. */
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
