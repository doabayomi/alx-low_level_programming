#include <stdio.h>

/**
 * _atoi - convert a string to an integer.
 * @s: the string to be comverted.
 *
 * Return: Some integer.
 */
int _atoi(__attribute__((__unused__)) char *s)
{
	/* Empty file */
	return (0);
}
