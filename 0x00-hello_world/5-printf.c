#include <stdio.h>

/**
 * main - The everyday main of C
 *
 * Description: A program that prints out a string to the standard output
 * using the printf() function
 * Return: zero value
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
